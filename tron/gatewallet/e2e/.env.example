# Gate Wallet-specific E2E environment variables.
# All shared configuration (extension path, password, chain, etc.) lives in tron/.env.
# Add overrides here only if Gate Wallet needs different values from the shared defaults.
