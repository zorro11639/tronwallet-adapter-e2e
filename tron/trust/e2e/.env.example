# Trust Wallet-specific E2E environment variables.
# All shared configuration (extension path, password, chain, etc.) lives in tron/.env.
# Add overrides here only if Trust Wallet needs different values from the shared defaults.
